This project has been built for use on windows. It has not been tested on other platforms.

The following files are required to enable download.file()'s "wget" option:
  - libeay32.dll
  - libiconv2.dll
  - libintl3.dll
  - libssl32.dll
  - wget.exe

Extract the files from wget.zip directly into the project directory.
